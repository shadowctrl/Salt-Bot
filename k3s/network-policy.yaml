apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: salt-bot-allow-internal
  namespace: murali
spec:
  podSelector: {}
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: murali
  policyTypes:
    - Ingress
