apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: murali
spec:
  selector:
    app: salt-postgres
  type: NodePort
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30432
